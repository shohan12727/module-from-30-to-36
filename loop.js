const countries = ['Bangladesh', 'India', 'South Africa', 'Srilanka', 'Pakistan', 'Australia'];

for(let i = 0; i< countries.length; i++) {
    const element = countries[i];
    console.log(element);
    
}

